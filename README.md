# herald-debian

Debian packaging for [herald](https://github.com/0443n/herald). Drop the source into this tree and build with `dpkg-buildpackage`.

## TODO

- Set up proper upstream tracking (uscan/watch, gbp)
